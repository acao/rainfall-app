import styles from '../styles'

export default {
  fullHeight: {
    outer: {
      backgroundColor: styles.palette.primary1Color,
      minHeight: '100%',
      margin: '0 auto',
      display: 'table'
    },
    inner: {
      backgroundColor: styles.palette.primary1Color,
      margin:0,
      textAlign: 'center',
      display: 'table-row'
    }
  }
}
